# Dependencies are declared in pyproject.toml.
#
# This file installs the project (editable) with every optional extra so the
# familiar `pip install -r requirements.txt` workflow still works.
#
# For a smaller install, use the extras directly, e.g.:
#   pip install -e '.[asr]'        # audio download + speech-to-text
#   pip install -e '.[analysis]'   # translation, topics, geo, PDF feedback
#   pip install -e '.[apis]'       # Gemini / ElevenLabs API clients
-e .[all]
